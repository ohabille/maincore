{
    "args" : {
        "keys" : [
            "request",
            "slug"
        ],
        "slug" : "([_a-zA-Z0-9]+)"
    },
    "pattern" : "(\\/([_\\-a-zA-Z0-9]+))",
    "default" : "home",
    "notFound" : "notFound"
}
